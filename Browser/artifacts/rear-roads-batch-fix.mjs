import {readFileSync,writeFileSync} from 'node:fs';
const p='Browser/test-annexe-rear-roads.mjs';let s=readFileSync(p,'utf8');s=s.replace('batchAerialMeshes(THREE,e);cacheAerialTransforms(THREE,e);','batchAerialMeshes(THREE,e.model,{exclude:[e.trees,e.terrain,...l.visibilityObjects]});cacheAerialTransforms(e.scene);');s=s.replace('const meshes=[];e.model.traverseVisible(o=>{if(o.isMesh)meshes.push(o);});\n  const expected','const meshes=[];l.historicRoads.traverseVisible(o=>{if(o.isMesh)meshes.push(o);});\n  const expected');s=s.replace("assert(hit,'Ground at '+p);","assert(hit||!expected,'Road or paving reaches '+p);");writeFileSync(p,s);
