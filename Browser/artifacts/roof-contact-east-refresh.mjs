import {execFileSync} from 'node:child_process';import {readFileSync,writeFileSync} from 'node:fs';import assert from 'node:assert/strict';
const run=args=>JSON.parse(execFileSync(process.execPath,args,{encoding:'utf8',windowsHide:true}));const old=run(['--import','./Browser/artifacts/roof-contact-before-loader.mjs','Browser/artifacts/roof-contact-east-snapshot.mjs']);const path='Research/annexe-east-outer/protected-before.json';assert.deepEqual(old,JSON.parse(readFileSync(path)));writeFileSync(path,JSON.stringify(run(['Browser/artifacts/roof-contact-east-snapshot.mjs']),null,2)+'\n');
