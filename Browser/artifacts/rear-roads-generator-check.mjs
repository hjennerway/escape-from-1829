import {readFileSync,writeFileSync} from 'node:fs';
const p='Browser/build-annexe-rear-roads.py';let s=readFileSync(p,'utf8');s=s.replace('# Rounded to micrometres so adjoining rings share identical serialized vertices.','# Serialize shared ring vertices without independent rounding at junctions.');s=s.replace("print('Merged asphalt area:'", "assert asphalt.is_valid and border.is_valid\nassert asphalt.intersection(walls).area < 1e-8\nassert asphalt.intersection(shared).area < 1e-8\nassert asphalt.intersection(border).area < 1e-8\nprint('Merged asphalt area:'");writeFileSync(p,s);
